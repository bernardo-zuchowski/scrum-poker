type Room {
  id: Int!
  title: String!
  users: [User!]!
  averageVote: Float!
}

extend type Query {
  rooms: [Room!]!
}

input CreateRoomInput {
  title: String!
  hostName: String!
}

extend type Mutation {
  createRoom(data: CreateRoomInput!): Room!
}