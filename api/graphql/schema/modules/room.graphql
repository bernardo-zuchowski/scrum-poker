type Room {
  id: Int!
  title: String!
  users: [User!]!
  averageVote: Float!
}

input CreateRoomInput {
  title: String!
  hostName: String!
}

extend type Mutation {
  createRoom(data: CreateRoomInput!): Room!
  updateRoom(id: Int!): Room!
}