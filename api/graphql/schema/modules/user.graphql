type User {
  id: Int!
  username: String!
  vote: Int
  isVoting: Boolean!
  isAdmin: Boolean!
}

extend type Query {
  users: [User!]!
}

input CreateUserInput {
  username: String!
  isAdmin: Boolean!
  roomId: Int!
}

extend type Mutation {
  createUser(data: CreateUserInput!): User!
}